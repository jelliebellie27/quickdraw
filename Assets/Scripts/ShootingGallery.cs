using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShootingGallery : MonoBehaviour
{
    typedef struct
    {
        void (#draw[3])();
    } board;

     typedef struct
    {
        int first;
        int second;
    } pair;

    typedef struct vector
    {
        void
        (#clear)(struct vector*);
        void
        (*delete)(struct vector*);
    size_t()

    class Exports :: Mswin <Exportsdef each_line(
}
